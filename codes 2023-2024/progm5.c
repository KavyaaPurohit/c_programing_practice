#include<stdio.h>
int main()
{
    int m,e,cs,p,c;
    printf("\nenter the maths marks");
    scanf("%d",&m);
    if(m>=90 && m<=100)
    {
        printf("the grade is AA");
    }
    else if(m>=80 && m<90)
    {
        printf("the grade is AB");
    }
    else if(m>=70 && m<80)
    {
        printf("the grade is BB");
    }
    else if(m>=60 && m<70)
    {
        printf("the grade is BC");
    }
    else
    {
    printf("the grade is f");    /* code */
    }
    printf("\nenter the english marks");
    scanf("%d",&e);
    if(e>=90 && e<=100)
    {
        printf("the grade is AA");
    }
    else if(e>=80 && e<90)
    {
        printf("the grade is AB");
    }
    else if(e>=70 && e<80)
    {
        printf("the grade is BB");
    }
    else if(e>=60 && e<70)
    {
        printf("the grade is BC");
    }
    else
    {
    printf("the grade is f");    /* code */
    }
    printf("\nenter the computer science marks");
    scanf("%d",&cs);
    if(cs>=90 && cs<=100)
    {
        printf("the grade is AA");
    }
    else if(cs>=80 && cs<90)
    {
        printf("the grade is AB");
    }
    else if(cs>=70 && cs<80)
    {
        printf("the grade is BB");
    }
    else if(cs>=60 && cs<70)
    {
        printf("the grade is BC");
    }
    else
    {
    printf("the grade is f");    /* code */
    }
    printf("\nenter the physics marks");
    scanf("%d",&p);
    if(p>=90 && p<=100)
    {
        printf("the grade is AA");
    }
    else if(p>=80 && p<90)
    {
        printf("the grade is AB");
    }
    else if(p>=70 && p<80)
    {
        printf("the grade is BB");
    }
    else if(p>=60 && p<70)
    {
        printf("the grade is BC");
    }
    else
    {
    printf("the grade is f");    /* code */
    }
    printf("\nenter the chemistry marks");
    scanf("%d",&c);
    if(c>=90 && c<=100)
    {
        printf("the grade is AA");
    }
    else if(c>=80 && c<90)
    {
        printf("the grade is AB");
    }
    else if(c>=70 && c<80)
    {
        printf("the grade is BB");
    }
    else if(c>=60 && c<70)
    {
        printf("the grade is BC");
    }
    else
    {
    printf("the grade is f");    /* code */
    } 
}